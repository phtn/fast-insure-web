import { LoaderIcon } from "lucide-react";

const LoaderMX = () => (
  <div className="flex h-full min-h-[50px] w-full items-center justify-center">
    <LoaderIcon className="size-4 animate-spin opacity-50" />
  </div>
);

export default LoaderMX;
